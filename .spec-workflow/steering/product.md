# Product Overview

## Related Documents

- **[docs/spec.md](../../docs/spec.md)** - 完全実装仕様書（技術詳細・コード例含む）

## Product Purpose

**最高のバイブコーディング体験を提供する**自律動作型MCPサーバー。

**核心理念: ユーザーは何もしなくていい**

会話中の重要情報をHooks連携で**自動保存**し、セッション開始時に**自動注入**することで、AIアシスタントが「学習する」体験を提供する。さらに、開発者が寝ている間にプロジェクトドキュメントを自動更新し、5時間レート制限ウィンドウを最適活用する。手動操作は完全にオプション。

### 解決する課題
1. **毎回同じミスをする** - API URL間違い、ポート間違い、ログを読まない等
2. **決定事項を忘れる** - 技術選定、アーキテクチャ決定が消える
3. **やったことを覚えていない** - 実装済み機能を再実装しようとする
4. **ユーザーの怒りを理解しない** - 同じ指摘を何度もさせる
5. **コンテキスト膨張** - セッションが長くなるとパフォーマンス劣化
6. **よく使うコマンド・クエリを毎回探す** - テストAPI、DBクエリ等を覚えていない
7. **Specドキュメントが陳腐化する** - 日々の改修でコードとドキュメントが乖離していく
8. **5時間レート制限が無駄になる** - 寝ている間のトークン枠が使われない
9. **コンテキスト注入の無駄** - セッション開始時に全記憶を注入すると、関係ない情報でコンテキストを圧迫する

## Target Users

### Primary User
- **Claude Code利用者**（開発者）
- 日常的にClaude Codeでプログラミングを行う
- プロジェクトごとに異なる設定・規約がある
- 同じミスの繰り返しにフラストレーションを感じている

### Pain Points
- 「何度言えば分かるの？」という体験
- 毎セッション、同じ説明を繰り返す手間
- 長いセッションでのレスポンス低下
- プロジェクト固有のルールが守られない
- よく使うテストコマンドを毎回教え直す手間

## Key Features

### 自動実行（Hooks連携・CLIスクリプト）
1. **wasurenagusa-context** - 【SessionStart / PreCompact Hook】config+dontを標準出力→コンテキスト注入。UserPromptSubmitイベントも受け付けるが何も出力しない（記憶想起はプロジェクト側hooksに移譲）
2. **wasurenagusa-analyze** - 【Stop Hook】会話を分析し、重要情報を自動保存

### MCPツール（AIが呼び出し）
3. **memory_get_context** - config+dont取得（Hooksでも自動実行）
4. **memory_search** - 軽量インデックス検索（タイトル+タグのみ返却でトークン節約）
5. **memory_get_detail** - 必要なエントリのフル詳細を取得

### 手動ツール（オプション）
6. **memory_save** - 明示的に保存したい時だけ使用
7. **memory_delete** - 不要エントリの削除（ID指定、複数一括削除対応）
8. **memory_update_intensity** - 既存エントリの重要度（intensity）更新。6以上でコンテキスト注入の最優先ピン留め

### 自律タスク実行（MCPツール）
9. **task_submit** - WHY/WHAT/DONE/PROJECTの4項目でタスクを投入。Geminiで命令文生成→Claude CLI実行→Gemini評価のパイプライン
10. **task_status** - タスク状態サマリ（pending/in-progress/completed/failed/human-required/cancelled件数）と直近タスク一覧
11. **task_action_list** - AIが「人間判断が必要」と仕分けたタスクの一覧と対応操作（retry/complete/cancel）
12. **project_init** - プロジェクトの品質基準・フェーズ・判断基準を選択式で登録。評価者エージェントの判定基準に反映

### Spec自動更新（Scheduler連携）
13. **wasurenagusa-spec-update** - 【cron/systemd timer】5時間5分サイクルでClaude Code CLIを起動し、Specドキュメントを自動更新
14. **変更ログ記録** - Stop Hook（wasurenagusa-analyze）実行時に、セッション中に変更されたファイル一覧を変更ログに記録
15. **タスクキュー** - 変更ベース更新・ローテーション更新・自律タスクを優先度付きキューで管理。全件dequeue後にmaxConcurrentTasks（デフォルト3）で並列数制限付き実行
16. **Keep-Alive** - タスクがない場合でもpingを送信し、5時間ウィンドウを回転させる

### コア機能
17. **LLM自動判定** - Genkit経由のマルチプロバイダー（Gemini/OpenAI/Anthropic、デフォルト: Gemini）で会話を自動分類・保存
18. **プロジェクト横断記憶** - シンボリックリンクで`.wasurenagusa/`を集約。各エントリにproject/scopeフィールドを持ち、フィルタリング可能
19. **AIリトライ検出** - AI自身の失敗パターンを自動検出・記録
20. **動的記憶取得** - agentモード: SessionStart時はdont原文+config+owner-profileのみ注入。話題関連記憶はAIがmemory_searchで動的に取得。injectionモード: 短期・中期・長期記憶を一括注入（従来方式）。記憶想起の仕組みはプロジェクト側hooksに移譲済み
21. **重複検出・自動マージ** - Geminiベースのセマンティック重複検出。同じテーマのエントリは自動置換でコンテキスト汚染を防止
22. **メタ情報による諦め検知** - メッセージ長の急減少やポジティブ反応の不在を数値で検知し、テキストパターンだけでは拾えない「沈黙の諦め」を捕捉
23. **プロンプト外部化** - 分析プロンプトを`prompts/`ディレクトリにテキストファイルとして管理。デプロイなしでプロンプト改善可能
24. **dont統合（L3圧縮）** - SessionStart時にGeminiで多数のdontエントリ（例:49件）を5-6個の行動原則に統合。元エントリは保持したまま、コンテキスト注入量を36KB→3-4KBに大幅削減
25. **Owner Profile** - SessionStart Hook初回実行時に`.wasurenagusa/owner-profile.md`を自動生成。優先順位・設計方針・コミュニケーションスタイル等を記入すると、SessionStart時のコンテキスト注入および自律タスク実行時の判断基準に反映
26. **Slack通知** - スケジューラサイクルサマリー（1サイクル1通に統合）/人間エスカレーション/リトライ上限到達/デイリーサマリーをSlack Webhook経由で通知（`SLACK_WEBHOOK_URL`環境変数で有効化、オプション）
27. **記憶想起リマインド** - 【UserPromptSubmit Hook】記憶想起機能はプロジェクト側のhooksに移譲。wasurenagusa-context内の`handleUserPromptSubmit()`は空実装（何も出力しない）
28. **Smart Tag Retrieval** - memory_save時にGemini APIで重み付きタグ（0.0〜1.0）を自動生成。検索時はfreshness（時間減衰）× tagWeightScore × accessBoostの複合スコアでランキング最適化。新テーマ検出時はバックグラウンドで過去エントリを非同期再タグ付け。データ削除は一切行わず、取り出し優先順位のみを最適化

## Memory Categories

| カテゴリ | 用途 | 例 |
|----------|------|-----|
| **config** | 設定情報 | API URL、ポート、認証情報、環境変数 |
| **dont** | やってはいけないこと | ユーザーが怒ったこと、過去のミス、禁止事項 |
| **decision** | 決定事項 | 技術選定、アーキテクチャ、命名規則 |
| **log** | 作業ログ | 実装した機能、解決したエラー |
| **snippet** | よく使うコマンド・クエリ | テストAPIコマンド、DBクエリ、便利スクリプト |

## Memory Metadata

各エントリには以下のメタデータが自動付与される：

| フィールド | 用途 | 取得方法 |
|-----------|------|---------|
| **project** | どのプロジェクトに紐づく知識か | cwdのディレクトリ名から自動取得 |
| **scope** | 知識の領域分類 | Gemini分析で自動判定 or 手動指定 |

### scope候補

| scope | 想定される知識 |
|-------|--------------|
| `frontend` | UI実装、状態管理、デザインパターン |
| `backend` | API設計、DB構造、サーバー処理 |
| `infra` | デプロイ、GCP設定、CI/CD、環境変数 |
| `design` | UI/UXの方針、デザインシステム |
| `spec` | 仕様決定、要件定義、ビジネスロジック |
| `ai` | LLM連携、プロンプト設計 |
| `general` | プロジェクト横断・分類不能（デフォルト） |

候補以外の自由入力も可。

## Use Cases

### UC1: ユーザーの怒りをトリガーとして記録
**シナリオ**: ユーザーが「なんでこんなこともわかんねーのよ」「さっき言ったでしょ」と怒る
**トリガー**: Geminiがユーザーの怒り・不満を検出
**アクション**: `dont`カテゴリに自動保存
**効果**: 次回以降、同じミスを事前に防止

### UC2: AI自身のリトライパターンを検出して記録
**シナリオ**: AIがAPIを何度も叩き直す、DBクエリを何回も発行する
**トリガー**:
- 同じAPIエンドポイントを短時間に3回以上実行
- 同じDBクエリを短時間に3回以上発行
- 同じエラーが繰り返し発生
**アクション**: `dont`カテゴリに「正しいやり方」を自動保存
**効果**: 次回は最初から正しい方法で実行

### UC3: よく使うテストAPIコマンドを保存
**シナリオ**: 「このAPIのテストはこのコマンドで」とユーザーが教える
**トリガー**: 手動保存 or Geminiが「これはよく使うコマンド」と判定
**アクション**: `snippet`カテゴリに保存
**効果**: memory_search "API テスト" で即座に取得可能

### UC4: プロジェクトの前提情報を保存
**シナリオ**: 「このプロジェクトはポート3000固定」「本番DBはFirestore」
**トリガー**: 手動保存 or Geminiが設定情報と判定
**アクション**: `config`カテゴリに保存
**効果**: memory_get_contextで毎セッション自動注入

### UC5: DBクエリのテンプレートを保存
**シナリオ**: 「ユーザー一覧取得はこのクエリ」とユーザーが教える
**トリガー**: 手動保存
**アクション**: `snippet`カテゴリに保存
**効果**: memory_search "ユーザー クエリ" で即座に取得可能

### UC6: Specドキュメントの自動更新（変更ベース）
**シナリオ**: 日中の作業で`src/api/handler.ts`と`src/models/user.ts`を改修した
**トリガー**: Stop Hook → wasurenagusa-analyzeが変更ファイルをログに記録 → 次のスケジューラサイクルで検出
**アクション**: Claude Code CLI（ヘッドレス）がプロジェクトのSpecドキュメントを変更内容に基づき更新
**効果**: 翌朝にはドキュメントが最新化されている

### UC7: Specドキュメントのローテーション更新
**シナリオ**: 変更ログはないが、プロジェクトAのSpecが30日前に最終更新
**トリガー**: スケジューラサイクルでタスクキューが空 → 最終更新日が古いプロジェクト順にローテーション
**アクション**: Claude Code CLIがコードベースとSpecの乖離を確認・更新
**効果**: 長期間触っていないプロジェクトのドキュメントも鮮度を保つ

### UC8: 5時間ウィンドウのKeep-Alive
**シナリオ**: 深夜帯で変更ログもローテーション対象もない
**トリガー**: 5時間5分サイクルのスケジューラ実行
**アクション**: `claude -p "ping"` で最小限のリクエストを送信
**効果**: 翌朝の作業開始時に5時間ウィンドウがリセット直後の状態になる

## Auto-Detection Triggers

### ユーザー起因トリガー
- **怒りの検出**: 「なんで」「何度言えば」「さっき言った」等のフレーズ
- **不満の検出**: 「違う」「そうじゃない」「だから」等のフレーズ
- **悲しみ・失望の検出**: 「期待してたのに」「残念」「がっかり」等のフレーズ
- **諦めの検出（最も深刻）**: 「もういいや」「自分でやる」+ メタ情報（メッセージ長の急激な減少、ポジティブ反応の長期不在）
- **明示的な指示**: 「覚えておいて」「これ忘れないで」「次からは〜」

### AI起因トリガー（リトライ検出）
- **API多重実行**: 同じエンドポイントを短時間に3回以上実行
- **DBクエリ多重発行**: 同じクエリを短時間に3回以上発行
- **エラー繰り返し**: 同じエラーが3回以上発生
- **ファイル多重編集**: 同じファイルを短時間に5回以上編集

## Business Objectives

- **開発者体験の向上**: Claude Codeとの会話品質を劇的に改善
- **トークンコスト削減**: 段階的開示で70-90%のトークン節約
- **学習するAI体験**: ユーザーの怒り・指摘を次回以降に活かす
- **ローカル完結**: 外部サービス依存なし、プライバシー保護
- **アイドルタイム活用**: 寝ている間の5時間ウィンドウでドキュメント自動保守
- **ドキュメント鮮度維持**: コードとSpecの乖離を自動的に解消

## Success Metrics

- **同じミスの再発率**: 80%減（dontカテゴリ活用）
- **セッション開始時の説明コスト**: 90%減（memory_get_context自動注入）
- **トークン消費量**: 従来比70%減（段階的開示）
- **ユーザー満足度**: 「賢くなった」と感じる率 > 80%
- **リトライ回数**: AI起因のリトライ50%減
- **Specドキュメント鮮度**: 最終更新から7日以内を維持（変更ベース更新 + ローテーション更新）
- **5時間ウィンドウ活用率**: アイドルサイクル率 < 20%（実タスク実行率 > 80%）

## Product Principles

1. **自律自動が基本、手動はオプション**: ユーザーは何もしなくても記憶が蓄積・活用される
2. **Hooks連携で完全自動化**: SessionStart/Stopで自動実行、ユーザー介入不要
3. **コンテキストを圧迫しない軽量設計**: 検索時はインデックスのみ返却、必要なものだけフル取得
4. **必要な時に思い出す**: agentモードではAIがmemory_searchで必要な記憶を動的に検索・取得。injectionモードではSessionStartで一括注入。どちらもdont+config+owner-profileは必ず注入。記憶想起トリガーはプロジェクト側hooksに移譲済み
5. **ローカル完結・セットアップ簡単**: npm installだけで動作、外部サービス最小限
6. **プロジェクト横断 + スコープ分類**: シンボリックリンクで`.wasurenagusa/`を集約管理。project/scopeフィールドでフィルタリング
7. **トリガーワード不要**: LLM（Gemini）で自動判定、自然な会話のまま学習
8. **AI自身の失敗も学習**: ユーザーの怒りだけでなく、AIのリトライパターンも検出

## Monitoring & Visibility

- **Dashboard Type**: なし（CLIツール、MCPプロトコル経由でのみアクセス）
- **Real-time Updates**: 不要（永続化されたMarkdownファイルを読み書き）
- **Key Metrics Displayed**: 検索結果件数、保存成功/失敗、タスク状態サマリ（task_status）
- **Notifications**: Slack Webhook経由でサイクルサマリー/人間エスカレーション/リトライ上限到達/デイリーサマリー通知（オプション）
- **Sharing Capabilities**: `.wasurenagusa/` ディレクトリをgit管理すればチーム共有可能

## Future Vision

### 将来の拡張可能性
- **SQLite移行**: 数千件規模への対応、検索性能向上
- **Embeddingによるセマンティック検索**: 類似内容の検出、重複マージ
- **HTTP Transport**: リモートアクセス、チーム共有
- **Analytics**: 保存頻度、検索パターン、学習効果の可視化
- **Collaboration**: チームでのdont/decisionの共有・投票
- ~~**タスクタイプ拡張**: Spec更新以外の自動タスク（セキュリティ監査、コードレビュー、TODO抽出等）~~ → task_submitで汎用タスク投入を実装済み
- **マルチプロジェクト並行**: 1サイクルで複数プロジェクトを効率的に処理
- ~~**重複検出・マージ**: 同じ内容の重複を自動検出し統合~~ → v0.3.0で実装済み（Geminiベース）
